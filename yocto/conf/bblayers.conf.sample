# Generated by `make yocto-setup`. The placeholder below is filled in with the absolute
# paths of the cloned layers, so this file stays portable across checkouts.

POKY_BBLAYERS_CONF_VERSION = "2"

BBPATH = "${TOPDIR}"
BBFILES ?= ""

BBLAYERS ?= " \
@LAYERS@
"
